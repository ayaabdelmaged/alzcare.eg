/**
 * Face embedding model identifiers — aligned with backend `constants/models.js` and ML runtime.
 */
export const FACE_MODEL_IDS = ['buffalo_l', 'facenet', 'vggface2'];

export const DEFAULT_MODEL_NAME = 'all';
